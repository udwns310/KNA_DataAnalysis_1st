# =====================================================================
# 조건문 (if) - 상황에 따라 다른 코드를 실행하기
# =====================================================================

# [개념] 순차 실행과 프로그램의 흐름
# - 프로그램은 기본적으로 위에서 아래로 한 줄씩 순서대로 실행
# - 코드를 쓴 순서가 곧 실행되는 순서
# - 조건문·반복문은 이 "위->아래" 흐름을 바꾸는 도구

# [개념] 흐름을 바꿔야 하는 순간 - "~하면 ~하고, 아니면 ~한다"
# - 일상: 비가 오면 우산을 챙기고, 안 오면 그냥 나감
# - 로그인: 비밀번호가 맞으면 입장, 틀리면 재입력 요청
# - 설비: 온도가 정상 범위를 벗어나면 경고, 정상이면 통과


# =====================================================================
# 불리언(bool)과 비교 연산자
# =====================================================================

# [개념] 참·거짓, 불리언 값
# - 참은 True, 거짓은 False
# - 대문자로 시작, 따옴표 없이 씀 ("True"는 문자열이라 다른 값)

# [개념] 비교 연산자로 조건식 만들기
# 연산자 | 의미        | 예시
# ==     | 같다        | 5 == 5   -> True
# !=     | 다르다      | 5 != 3   -> True
# >  / < | 크다 / 작다  | 5 < 8    -> True
# >= / <=| 크거나/작거나 같다 | 5 >= 5 -> True
#

# [강사님께 질문하기] 실수(float) 값을 == 로 비교하면 오차 때문에 위험하다고 들었는데,
# 센서 데이터처럼 실수를 다루는 이번 과정에서는 어떻게 비교하는 게 안전한가요?

print(10 > 3)  # True
print(10 < 3)  # False

pressure = 85
print(pressure > 80)  # True  (85는 80보다 크다)

pressure = 70
print(pressure > 80)  # False (70은 80보다 크지 않다)


# =====================================================================
# if 문의 기본 구조
# =====================================================================

# [개념] if 문 기본 구조 - "만약 ~하면"
pressure = 85  # 측정 압력
if pressure > 80:  # 만약 압력이 80을 넘으면
    print("압력 주의")  # 들여 쓴 이 줄만 실행
print("측정 종료")  # if와 상관없이 항상 실행

# [개념] 콜론(:)과 코드 블록
# - 콜론은 "여기부터 이 조건에 속하는 코드 시작"이라는 신호
# - 그 아래 들여 쓴 코드 한 덩어리가 "코드 블록"
# - if, elif, else 모두 콜론으로 끝나는 공통 규칙 (반복문도 동일)

# [개념] 들여쓰기와 실행 범위
pressure = 70
if pressure > 80:
    print("압력 주의")  # 들여쓰기 안 -> 조건 참일 때만 (지금은 실행 안 됨)
    print("점검 필요")  # 같은 블록 -> 역시 조건 참일 때만
print("측정 종료")  # 들여쓰기 밖 -> 항상 실행

# [주의] 들여쓰기 오류 두 가지
# - 오류 발생형: if 다음 줄을 아예 들여 쓰지 않으면 IndentationError로 바로 멈춰서 알아채기 쉬움
# - 조용한 오작동형: 둘째 줄만 들여쓰기가 풀리면 오류 없이 "항상" 실행되어 결과만 틀림 -> 발견하기 어려움


# =====================================================================
# 조건 판단 설계 3단계 & 경계 표현
# =====================================================================

# [개념] 조건 판단 설계 3단계
# ① 상황 파악: 판단할 값 찾기 (예: 압력)
# ② 조건 정의: 기준을 비교식으로 (pressure 80 초과)
# ③ 결과 지정: 참일 때 할 일 (경고 출력)

# [정리] 조건 정의의 핵심 - 경계 표현
# 우리말 | 기호 | 경계값 포함
# 이상   | >=   | 포함
# 초과   | >    | 미포함
# 이하   | <=   | 포함
# 미만   | <    | 미포함

age = input(" 나이를 입력해주세요 : ")
if int(age) >= 20:
    print("성인입니다.")
else:
    print("미성년자입니다.")

answer = 7
digit = int(input("1~10 사이의 숫자를 맞춰보세요 : "))
if digit == answer:
    print("맞았습니다.")
else:
    print("틀렸습니다.")

traffic_light = input("신호등 색상을 입력해주세요 (빨간색, 초록색) : ")
if traffic_light == "빨간색":
    print("기다리세요")
if traffic_light == "초록색":
    print("건너세요")
if traffic_light != "빨간색" and traffic_light != "초록색":
    print("다시 입력해주세요")
    
print(
    "\n===================== 실습 1. 근속연수로 정규직 전환 여부 판정하기 ====================="
)
# 목표: if-else 로 조건 하나를 판정해 두 경우로 나눠 출력하기
# 단계: ① 근속연수를 정수로 입력받아 변수에 저장 -> ② 비교 연산자로 3년 이상 기준의 조건식을 만들기
#      -> ③ if 에 참일 때 출력, else 에 거짓일 때 출력을 작성 -> ④ 3, 2를 넣어 경계에서 바르게 갈리는지 확인
# 예상 결과: (3 입력) 전환 대상입니다 / (2 입력) 전환 대상이 아닙니다
years = int(input("근속연수를 입력하세요: "))
if years >= 3:
    print("전환 대상입니다")
else:
    print("전환 대상이 아닙니다")


# =====================================================================
# else / elif
# =====================================================================

# [개념] else로 나머지 경우 처리
# - else는 if 조건이 거짓일 때 실행할 코드를 담음
# - else에는 조건식을 따로 쓰지 않음 (나머지 모두를 맡음)
# - if와 else 중 항상 하나만 실행

score = 68
if score >= 60:  # 68 >= 60 -> 참
    print("합격")  # 이 줄만 실행
else:
    print("불합격")  # 건너뜀
# 출력 결과: 합격

# [개념] elif로 여러 갈래 판단
# - elif는 갈림길이 셋 이상일 때 중간 조건을 추가
# - 앞 조건이 거짓일 때만 다음 elif 조건을 검사
# - 필요한 만큼 여러 개 이어 붙이기 가능

# [개념] if-elif-else 전체 구조
score = 82
if score >= 90:
    print("우수")  # 거짓
elif score >= 70:
    print("보통")  # 참 -> 실행
else:
    print("미흡")

# [흔한 실수] elif 순서의 함정
# Q. '60 이상이면 보통'을 맨 위에 두면 95점은 무슨 등급?
# -> A. 보통. 95도 60 이상이라 첫 조건에서 걸려버려 우수가 나오지 않음
# Q. 그렇다면 조건을 어떤 순서로 배치해야 하나?
# -> A. 더 좁고 까다로운 조건(90 이상)을 위에, 넓은 조건(60 이상)을 아래에

# [강사님께 질문하기] elif가 5개, 10개로 길어지면 가독성이 떨어지는데,
# 실무에서는 어떻게 정리하나요? (딕셔너리로 매핑하기, match-case 문법 등 대안이 있는지)


# =====================================================================
# 논리 연산자 and / or / not
# =====================================================================

# [정리] 논리 연산자 and와 or
# 조건 A | 조건 B | A and B | A or B
# 참     | 참     | 참       | 참
# 참     | 거짓   | 거짓     | 참
# 거짓   | 참     | 거짓     | 참
# 거짓   | 거짓   | 거짓     | 거짓

# [개념] 논리 연산자 not
# - not은 참을 거짓으로, 거짓을 참으로 뒤집음
# - '~가 아닐 때'를 표현 (not is_open -> 열려있지 않으면)
# - and·or와 섞을 때는 괄호로 범위를 분명히

# [강사님께 질문하기] and/or의 "단락 평가(short-circuit)"가 실무 코드에서 성능이나
# 안전성 때문에 중요하게 쓰이는 실제 사례가 있나요? (앞 조건이 거짓이면 뒤 조건은 계산 안 함)

# [개념] 범위 조건과 복합 조건
noise = 45
if noise >= 20 and noise <= 60:
    print("정상 범위")
if 20 <= noise <= 60:  # 위와 같은 뜻을 더 짧게 쓴 표현
    print("정상 범위")

# [정리] =와 == 혼동 피하기
# 기호 | 이름 | 하는 일        | 예시
# =    | 대입 | 값을 변수에 담기 | x = 5
# ==   | 비교 | 같은지 따지기    | x == 5


# =====================================================================
# 조건문 코드 읽기 절차 (디버깅에 그대로 적용)
# =====================================================================

# [개념] 조건문 코드 읽기 절차
# ① 변수 값 확인: 조건에 쓰인 변수의 현재 값 찾기
# ② 조건 대입: 위에서부터 넣어 처음 참인 조건 찾기
# ③ 블록 추적: 참인 블록의 들여 쓴 코드 따라가기

# [개념] 디버깅에 적용하기
# - 증상 확인: 기대한 "주의" 대신 "정상"이 출력됨
# - 값 점검: 변수에 어떤 값이 들어갔는지 위쪽에서 확인
# - 원인 발견: 조건 순서나 비교 기호 오타(80 vs 800)를 추적해 찾아냄


print("\n===================== 실습 2. 설비 압력 상태 판정하기 =====================")
# 목표: if-elif-else 로 측정 압력을 위험·주의·정상으로 판정하기
# 단계: ① 측정 압력을 정수로 입력받아 저장 -> ② 90 초과·70 초과 기준을 높은 값부터 elif 로 배치
#      -> ③ 각 구간에 위험·주의를 출력하고 나머지는 else 로 정상 출력 -> ④ 95, 75, 50을 넣어 결과 확인
# 예상 결과: 95 -> 위험 / 75 -> 주의 / 50 -> 정상
press = int(input("측정 압력을 입력하세요: "))
if press > 90:
    print("위험")
elif press > 70:
    print("주의")
else:
    print("정상")


print(
    "\n===================== 실습 3. 두 조건을 모두 만족하는지 검증하기 ====================="
)
# 목표: and 로 두 조건을 모두 만족할 때만 통과시키기
# 단계: ① 올바른 아이디·비밀번호를 변수로 저장 -> ② 사용자 입력을 각각 받아 저장
#      -> ③ 두 비교를 and 로 묶어 모두 참일 때만 성공, 아니면 else 로 실패 출력
#      -> ④ 맞는 값과 틀린 값을 각각 넣어 확인
# 예상 결과: (manager/5678) 로그인 성공 / (틀린 값) 로그인 실패
correct_id = "admin"
correct_pw = "1234"
user_id = input("아이디: ")
user_pw = input("비밀번호: ")
if user_id == correct_id and user_pw == correct_pw:
    print("로그인 성공")
else:
    print("로그인 실패")


# =====================================================================
# 중첩 if - 조건 안의 조건
# =====================================================================

# [개념] 중첩 if
speed = 78
noise = 3.5
if speed > 60:  # 1차: 속도가 높은가
    if noise > 3.0:  # 2차: 소음까지 높은가
        print("고속·고소음 -> 위험")
    else:
        print("고속 -> 주의")
else:
    print("정상")

# [강사님께 질문하기] 조건이 여러 설비값(온도·압력·소음 등)을 동시에 볼 때, if를 이렇게
# 중첩하는 것과 and로 한 줄에 묶는 것 중 실무에서 어떤 기준으로 선택하나요?


print(
    "\n===================== 실습 5. 세 값으로 설비 종합 상태 판정하기 (도전) ====================="
)
# 목표: 중첩 if와 논리연산(and·or)으로 세 값을 단계적으로 판정하기
# 단계: ① 압력·소음·전압을 입력받아 저장 -> ② 압력>95 또는 소음>85면 즉시 '위험: 즉시 정지'
#      -> ③ 위험이 아니면 전압 > 220 and 압력 > 85면 '주의: 전압 점검', 소음>70이면 '주의: 소음 관찰',
#         아니면 '정상' -> ④ 88 / 75 / 200을 넣어 확인
# 예상 결과: 88 / 75 / 200 -> 주의: 소음 관찰
press = int(input("압력: "))
noise = float(input("소음: "))
voltage = int(input("전압: "))
if press > 95 or noise > 85:  # 1차: 하나라도 한계 초과면 즉시 위험
    print("위험: 즉시 정지")
else:  # 위험이 아닌 값만 2차 세부 판정
    if voltage > 220 and press > 85:
        print("주의: 전압 점검")
    elif noise > 70:
        print("주의: 소음 관찰")
    else:
        print("정상")

# [강사님께 질문하기] 지금은 조건문으로 한 줄씩 판정하지만, 나중에 pandas로 대량의 설비
# 데이터를 다룰 때도 if-elif 로직이 그대로 이어지나요, 아니면 다른 방식(조건 필터링 등)으로 바뀌나요?


# =====================================================================
# [흔한 질문 진단] 조건문 다루면서 헷갈리기 쉬운 것들
# =====================================================================

# Q1. elif 여러 개 중에 조건에 맞는 게 두 개 이상이면 둘 다 실행되나요?
# -> A. 아니요. 위에서부터 검사해서 "처음 참인 것 하나"만 실행하고 나머지는 검사조차 안 함
#       독립적으로 각각 검사하고 싶으면 elif가 아니라 if를 따로따로 여러 번 써야 함
level = 85
if level >= 60:
    print("A")  # 이것만 실행
elif level >= 80:
    print("B")  # 조건은 참이지만 위에서 이미 걸렸으므로 검사조차 안 됨

# Q2. 파이썬에서 if x = 5: 처럼 =을 실수로 쓰면 어떻게 되나요?
# -> A. C·자바와 달리 파이썬은 이 경우 "조용히 잘못된 값이 대입"되는 게 아니라
#       아예 SyntaxError로 실행이 멈춤 (if 조건 자리에 대입문 = 을 쓰는 것 자체가 문법 오류)
#       즉 =/== 혼동은 파이썬에서는 대부분 바로 알아챌 수 있는 오류
# if x = 5:      # SyntaxError: invalid syntax (주석 처리 - 실행하면 바로 오류)
#     print(x)

# Q3. 들여쓰기 오류는 항상 IndentationError로 바로 알 수 있나요?
# -> A. 아니요. if 블록 안의 "일부 줄"만 들여쓰기가 풀리면 오류 없이 "항상 실행"되어 버림
#       코드는 멀쩡히 돌아가지만 조건과 상관없이 실행되는 조용한 버그라 발견이 더 어려움
speed = 10
if speed > 60:
    print("고속 경고")  # 조건 참일 때만
print("측정 완료")  # 들여쓰기가 없어서 speed 값과 상관없이 항상 실행됨

# [강사님께 질문하기] 들여쓰기가 이렇게 조용히 풀려서 생기는 버그를 실무에서는
# 어떤 도구나 습관으로 미리 잡아내나요? (린터, 자동 포맷터 등)

# Q4. and와 or를 함께 쓰면 어떤 순서로 계산되나요?
# -> A. not > and > or 순으로 먼저 계산됨. 의도한 범위와 다르게 묶일 수 있어
#       조건이 셋 이상 섞이면 괄호로 범위를 직접 표시하는 습관이 안전함
a, b, c = True, False, True
print(a or b and c)  # and가 먼저: b and c(False) -> a or False -> True
print(
    (a or b) and c
)  # 괄호로 순서를 바꾸면 결과가 달라질 수 있음 -> True (우연히 같음, 값 바꿔보며 확인)

# [퀴즈] 아래 코드의 출력 결과를 먼저 예상해보기
# level = 72
# if level >= 90:
#     print("S")
# elif level >= 70:
#     print("A")
# elif level >= 60:
#     print("B")
# else:
#     print("C")
# 정답: A  (72는 90 미만이라 첫 조건 탈락 -> 70 이상이라 두 번째 elif에서 걸려 실행되고 종료)

# [퀴즈] 다음 중 SyntaxError가 나는 줄은?
# ① if x == 5:
# ② if x = 5:
# ③ if x != 5:
# 정답: ②  (조건 자리에는 비교식만 올 수 있고, 대입식 =은 문법 오류)


# =====================================================================
# [추가 학습] 조건에 숫자·문자열·리스트를 바로 넣으면? (Truthy/Falsy 미리보기)
# =====================================================================
# - True/False가 아닌 값도 if 조건 자리에 들어가면 파이썬이 참·거짓으로 자동 판단함
# - 0, 빈 문자열 "", 빈 리스트 []는 거짓(Falsy) 취급, 그 외 대부분은 참(Truthy) 취급
count = 0
if count:
    print("데이터 있음")
else:
    print("데이터 없음")  # count가 0이라 Falsy -> 이 줄 실행

readings = []
if readings:
    print("측정값 있음")
else:
    print("측정값 없음")  # 빈 리스트라 Falsy -> 이 줄 실행
