import os
import pandas as pd
import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt
import seaborn as sns

sns.set_theme(style="whitegrid")
plt.rcParams["font.family"] = "Malgun Gothic"
plt.rcParams["axes.unicode_minus"] = False


# =====================================================================
# 제조 현장과 설비 구조 - 철강 공정 개관
# =====================================================================

# [문제 상황] 우리가 다룰 숫자는 컴퓨터가 만든 게 아니라 쇠붙이가 움직여서 남긴 흔적
# - 진동 값 하나도 실제 모터축 회전 -> 진동 센서의 신호 변환 -> CSV 저장이라는 3단계를 거쳐서야
#   화면의 숫자가 됨 - 공정을 모르면 이 숫자를 엉뚱하게 해석하게 됨
# - 대표적인 세 가지 오해: ① 고착 오판(새벽에 값이 안 움직임 -> 설비가 서 있었을 뿐인데 고장으로 오인)
#   ② 주기 오판(하루 여덟 번 급락 -> 정기 작업 주기인데 이상으로 오인) ③ 인과 오판(두 값이 함께 오름 ->
#   실은 같은 원인에 반응한 결과일 뿐인데 한쪽이 다른 쪽 원인이라 오인)
# - 오해를 푸는 세 정보: 가동 여부(그 시각에 실제로 돌고 있었나) · 고유 주기(원래 어떤 리듬으로 움직이나)
#   · 연결 관계(다른 설비와 어떻게 이어져 있나)

# [개념] 공정 · 설비 · 라인 - 크기가 다른 세 단위
# - 공정: 재료 상태를 바꾸는 단계 - 데이터에서는 손실이 계산되는 단위
# - 설비: 변화를 일으키는 실제 기계 - 데이터에서는 센서가 붙는 단위
# - 라인: 설비가 이어진 배열 - 데이터에서는 정지가 퍼지는 범위
# - 계층 구조: 설비 -> 라인 -> 공정 -> 공장으로 갈수록 큰 단위 - "공정"이라는 말도 제선·제강처럼
#   공장 단위를 가리킬 때와 장입·취련처럼 개별 작업을 가리킬 때 크기가 다르므로 맥락 확인이 필요

# [개념] 이 강의가 쓰는 실습 데이터의 성격
# - 각색한 것: 컬럼 이름과 설비 이름을 철강 현장 방식에 맞춰 변경
# - 유지한 것: 결측 발생 방식, 값이 튀는 모양, 라벨 불균형 - 실제 공장 데이터의 "질감"은 그대로
# - 출처: 공개 제조 데이터셋과 강사 제작 학습용 자료


# [퀴즈] 새벽 2시에 어떤 설비의 온도 데이터가 두 시간 내내 똑같은 값으로 멈춰 있다면?
# 정답: 반드시 고장은 아님 - 그 시간에 설비가 아예 꺼져 있었을 가능성이 더 큼
# (가동 여부를 먼저 확인해야 함 - 안 도는 설비는 진동도 온도 변화도 없는 게 오히려 정상)


# [정리] 왜 하필 철강 공정을 예시로 쓰는가
# - 회전·유압·열·반송 네 가지 설비 유형을 모두 갖춰 다른 업종(반도체 진공펌프, 식품 냉동기,
#   데이터센터 냉각팬도 결국 이름만 다른 회전기계)에도 그대로 적용 가능
# - 정지 비용 계산이 뚜렷해 예지보전 사례가 풍부하고, 연속공정(고로)과 배치공정(코크스로)을
#   한 자리에서 비교 학습 가능
# - 가져갈 것은 용어 자체가 아니라 "무엇을 하는 물건이고, 어떤 물리량으로 상태를 보고, 서면
#   어디까지 영향을 주는가"라는 질문 세트


# =====================================================================
# 철강 공정 전 구간 지도
# =====================================================================

# [개념] 일관제철소 vs 전기로 제철소 - 원료 상태가 공정 구성을 결정
# - 일관제철소(고로 방식): 철광석의 산소를 제거해 철 추출 - 제선·제강·연주·압연 4단계, 데이터는
#   끊김 없는 연속 사이클
# - 전기로 제철소: 고철을 전기 아크로 재용해 - 제선 없이 제강·연주·압연 3단계, 데이터는 사이클
#   경계가 뚜렷 - 고로 공장은 태그 수가 많고 정지 사례가 드물어 클래스 불균형이 심하지만,
#   전기로 공장은 사이클 단위로 구간을 나누기가 오히려 쉬움
# - 국내 일관제철소는 포항·광양·당진 세 곳, 광양 5개 고로가 연간 약 1,800만 톤 규모,
#   고로 높이는 100m를 넘고 압연 라인은 수백 m - 사람 눈으로 감시가 불가능한 규모라 데이터
#   분석이 필요해짐

print(">>> 같은 철이 이름을 바꿔가며 6단계를 거치는 과정")
process_steps = pd.DataFrame({
    "단계": ["원료 전처리", "제선", "제강", "연주", "압연", "후처리/출하"],
    "설비": ["소결/코크스 공장", "고로", "전로", "연주기", "압연기", "-"],
    "산출물": ["소결광·코크스", "용선(쇳물)", "용강", "슬래브 등 반제품", "열연/냉연코일·후판", "최종 철강제품"],
    "재료상태": ["덩어리", "액체", "정제된 액체", "굳은 반제품", "고체 판", "최종 형태"],
})
print(process_steps.to_string(index=False))

# [개념] 상공정과 하공정 - 위아래가 아니라 강물의 상류·하류
# - 경계는 연주 - 상공정은 쇳물(가루·액체)을, 하공정은 굳은 쇠(고체 판·코일)를 다룸
# - 재료상태: 상공정 가루·액체(1000~1600도) vs 하공정 고체 판·코일(상온~1000도 이상)
# - 변화속도: 상공정은 시간 단위로 완만 vs 하공정은 초·밀리초 단위로 빠름
# - 품질측정: 상공정은 시료 분석이라 띄엄띄엄 vs 하공정은 라인 위 연속 계측
# - 정지 난이도: 상공정은 매우 어려움(액체를 멈추면 굳어버림) vs 하공정은 상대적으로 쉬움
# - 필요 지식: 상공정은 화학과 야금 vs 하공정은 기계와 제어

# [정리] 이름이 바뀌는 지점의 의미
# - 철광석 -> 소결광 -> 용선 -> 용강 -> 슬래브 -> 코일, 이름이 바뀌는 지점마다 되돌릴 수 없는
#   변화가 일어남 - 그 비가역 경계를 알면 품질 문제가 생겼을 때 어디까지 거슬러 올라가 원인을
#   찾아야 하는지(상류 범위)가 명확해짐
# - 반제품은 슬래브(넓적한 판, 열연·후판행)·블룸(굵은 각기둥, 형강행)·빌릿(가는 각기둥, 철사·
#   철근행) 세 가지 - 종류가 다르면 주조 속도·냉각수 양·몰드 크기도 달라서, 종류를 구분하지 않고
#   데이터를 합치면 서로 다른 정상 조업 범위가 한 데이터에 뒤섞임


# =====================================================================
# 설비 태그 읽기
# =====================================================================

# [개념] 물질 흐름과 데이터 흐름의 대응
# - 원료(원료 야드와 반송 태그) -> 소결(소결 설비와 품질 태그) -> 고로(제선 조업과 용선 태그) ->
#   전로(정련 조건과 용강 태그) -> 연주(주조 조건과 반제품 태그) -> 압연(형상·두께·표면 품질 태그)
# - 세 가지 대응 관계: 공정 단계 대 데이터 그룹(공정별로 묶으면 수만 개 태그가 관리 가능한 크기로
#   축소) / 이동 시간 대 데이터 지연(앞뒤 값을 비교하려면 이동 시간만큼 시간을 밀어 정렬) /
#   공정 산출물 대 라벨 후보(각 공정 끝의 품질 판정·등급이 지도학습의 정답이 됨)
# - 상관관계를 발견했을 때 어느 쪽이 앞 공정인가를 먼저 확인하는 습관 - 통계보다 공정도가 먼저

# [개념] 설비 태그명의 다섯 마디 - PL1-BF-BLW-01-VIB
# - ① 공장(PL1) ② 공정 약어(BF=고로) ③ 설비 종류(BLW=송풍기) ④ 호기 번호(01) ⑤ 계측 항목(VIB=진동)
# - 공정 약어표: SNT=소결(Sinter), CKO=코크스로(Coke Oven), BF=고로(Blast Furnace),
#   BOF=전로, CCM=연주(Continuous Casting), HSM=열간압연(Hot Strip Mill), CRM=냉간압연,
#   UTL=유틸리티(Utility)

print("\n>>> 태그 문자열을 다섯 마디로 쪼개기")
tag = "PL1-BF-BLW-01-VIB"
plant, proc, equip, no, meas = tag.split("-")
print(plant, proc, equip, no, meas)

print("\n>>> 실제 설비 태그 24개 불러오기")
tags = pd.read_csv("data/01-01_철강_공정_개관_설비태그.csv")
print(tags.shape)
print(tags.head())

print("\n>>> 태그를 다섯 컬럼으로 분해")
parts = tags["tag"].str.split("-", expand=True)
parts.columns = ["plant", "proc", "equip", "no", "meas"]
tags = pd.concat([tags, parts], axis=1)
print(tags[["tag", "plant", "proc", "equip", "no", "meas"]].head())

print("\n>>> 공정 약어별 태그 개수")
proc_count = tags["proc"].value_counts()
print(proc_count)

# [강사님께 질문하기] tag.split("-")로 다섯 조각을 한 번에 나눌 수 있다는 건 태그 이름 안에
# 구분자가 하이픈뿐이라는 뜻인데, 실제로는 설비 이름 자체에 하이픈이 들어가는 경우는 없나요?
# -> 답변: 좋은 지적. 실제 태그 체계에서는 이런 충돌을 피하려고 "구분자로 쓰는 문자는 각 마디
#    내부에서는 절대 쓰지 않는다"는 규칙을 미리 정해 둠(마디 내부는 영문 대문자·숫자만 허용하는
#    식). split("-")가 안전하게 동작하는 건 우연이 아니라 태그를 설계할 때부터 지킨 약속 덕분 -
#    반대로 이런 약속이 없는 현장 데이터라면 split 전에 반드시 구분자 규칙부터 확인해야 함

# [정리] 태그 구조가 유용한 세 가지 이유
# - 필터링: 두 번째 마디로 특정 공정 태그만 몇 줄 문자열 처리로 선별
# - 그룹핑: 앞 네 마디(공장·공정·설비·호기)가 같으면 같은 설비의 다른 계측 항목
# - 결측 해석: 공정 단위로 통째로 결측이면 설비 정지, 계측 항목 하나만 결측이면 센서 문제로 구분


# =====================================================================
# 원료와 통기성
# =====================================================================

# [개념] 원료가 도착하는 상태와 세 가지 변동성
# - 철광석의 철 성분은 산소와 결합한 산화철 형태 - 제선은 이 산소를 제거해 철을 얻는 과정(환원)
# - 철광석은 입자 크기와 성분이 균일하지 않고, 이 차이가 고로 내부 가스 흐름과 반응에 영향을 줌
# - 세 가지 변동성: 산지 변동(호주·브라질 등 산지별 성질 차이) / 배합 변동(여러 산지 광석을
#   섞는 비율 변화) / 취급 변동(이송 중 덩어리가 깨지는 분화)

# [개념] 통기성 - 원료층 사이로 기체가 얼마나 잘 지나갈 수 있는지
# - 하나의 센서로 직접 측정하기보다 풍량·압력 등 여러 값의 관계로 추정
# - 통기성을 결정하는 네 요소: 입자 크기(일정 크기 이상 vs 분광 비율 증가) / 크기 균일성
#   (입도 분포 균일 vs 체 선별 불량) / 입자 강도(낙하에도 안 부서짐 vs 소결광 강도 저하) /
#   층 형상(균일한 두께 vs 장입 편심·쏠림)
# - 추정 원리: 다른 조업 조건이 비슷할 때 같은 풍량에 더 큰 압력이 요구되면 통로가 좁아졌다는
#   신호 - 계산식은 달라도 풍량과 압력을 함께 보는 발상은 소결 공정에도 동일하게 적용됨

# [개념] 철광석 가루를 고로에 그대로 못 넣는 이유, 그리고 세 가지 원료 형태
# - 통로 막힘: 가루가 원료 사이의 빈 공간을 메워 가스 통로를 좁힘
# - 비산 손실: 가벼운 가루가 반응 전에 가스를 타고 상부로 유실
# - 그래서 고로에는 소결광(가루를 구워 붙인 덩어리, 구멍이 많아 공기가 잘 통함) · 펠릿(고운
#   가루를 구슬 모양으로 뭉쳐 구운 것, 크기가 고르고 안 부서짐) · 괴광(캐낼 때부터 덩어리인
#   철광석, 부수거나 선별해 크기를 맞춤) 세 형태로 넣음

# [퀴즈] 철광석 가루를 씻지도 거르지도 않고 그대로 고로에 부으면 어떤 일이 먼저 벌어질까?
# 정답: 가루가 원료 사이 빈틈을 메워 가스가 통과할 길부터 좁아짐(통로 막힘)
# (동시에 가벼운 가루 일부는 반응하기도 전에 상승 가스를 타고 위로 날아가 버림(비산 손실) -
#  두 문제 모두 "가루라서" 생기는 문제이지 철 성분이 부족해서가 아님)

# [개념] 석회석이 슬래그를 만드는 과정과 슬래그의 두 가지 역할
# - 철광석 속 규소·알루미늄 같은 불순물은 철이 되지 않고 그대로 남음 -> 석회석의 칼슘 성분이
#   이 불순물과 결합해 분리 가능한 상을 형성 -> 쇳물보다 가벼운 슬래그가 위로 떠올라 별도 배출
# - 역할 ①: 불순물 그릇 - 규소·알루미늄을 모아 쇳물과 분리, 너무 걸쭉하거나 묽지 않게 점성 관리
# - 역할 ②: 조성과 유동성 관리 - 쇳물 표면을 덮어 공기 접촉을 차단, 고온 쇳물의 재산화 억제


# =====================================================================
# 소결과 코크스
# =====================================================================

# [개념] 소결 3단계와 화염 이동 3단계
# - 배합(가루 철광석·부스러기·석회석·연료를 정해진 비율로 섞기) -> 조립(물을 뿌려 가루를
#   작은 알갱이로 뭉치기 - 여기서 통기성이 확보됨) -> 장입(넓은 판 위에 같은 두께로 깔기)
# - 점화(맨 위쪽에 불 붙이기) -> 흡인(판 아래에서 공기를 빨아들이며 불이 위에서 아래로 서서히
#   내려감) -> 소결광 완성(가루끼리 표면이 붙어 구멍 많은 덩어리, 부수고 크기별로 골라 고로로)
# - 소결에서도 고로에서도 답은 같음 - 바람이 지나갈 길을 만들어 주는 것(통기성)이 두 공정을
#   잇는 핵심 개념

# [개념] 소결 조업의 네 가지 변수 - 서로 얽혀 있어 한 번에 여러 값이 같이 움직임
sinter_vars = pd.DataFrame({
    "변수": ["연료 비율", "수분", "대차 속도", "층 두께"],
    "높을 때": ["강도 상승·과용융 위험", "질척한 층과 통기성 저하", "생산량 증가·미소결 위험", "처리량 증가·화염 도달 지연"],
    "낮을 때": ["미소결, 강도 저하", "의사입자 형성 불량", "생산량 감소", "처리량 감소·도달 시간 단축"],
})
print("\n>>> 소결 조업 4대 변수")
print(sinter_vars.to_string(index=False))
# - 여러 변수가 동시에 움직이면 설비 이상이 아니라 조업 조정일 가능성이 큼 - 조작값은 원인이
#   아니라 이미 무언가에 대응한 결과일 수 있다는 점을 늘 의심하기

# [개념] 코크스의 세 역할과 코크스로의 구조
# - 코크스 역할: 연료(타면서 열 공급) / 환원제(탄소가 철광석의 산소를 떼어냄) / 지지대(원료가
#   녹아 흘러도 고체로 남아 가스 통로를 유지)
# - 코크스로가 하는 일은 "고로 안에서 일어나면 곤란한 변화를 미리 밖에서 일으켜 두는 것" -
#   소결 공장이 가루를 미리 덩어리로 만드는 것과 같은 논리
# - 탄화실(배합탄이 들어가는 밀폐된 방, 산소가 없어 타지 않고 건류만 진행) + 연소실(탄화실
#   사이에서 연료 가스를 태워 가열벽으로 간접열 전달)

# [정리] 배치 설비(코크스로) vs 연속 설비(고로)의 데이터 모양
batch_vs_continuous = pd.DataFrame({
    "비교항목": ["조업 방식", "데이터 모양", "정상 판정"],
    "코크스로": ["방 단위로 채우고 비움", "주기적 상승·하강 반복", "주기 모양이 유지되는지"],
    "고로": ["계속 넣고 계속 뽑음", "완만한 연속 변동", "값이 범위 안에 있는지"],
})
print(batch_vs_continuous.to_string(index=False))
# - 코크스로 데이터가 어려운 이유: 수십 개 방(탄화실)마다 낡은 정도가 다르고, 탄화 시작 시각이
#   달라 시간축을 그냥 겹칠 수 없으며, 방 안 온도를 직접 재기 어려워 연소실 온도로 간접 추정함

# [강사님께 질문하기] 코크스 강도가 낮아지면 고로 안에서 코크스가 부스러져 통기성이 나빠진다고
# 배웠는데, 코크스 강도 자체는 고로 데이터가 아니라 코크스로 쪽에서 미리 측정되는 값 아닌가요?
# -> 답변: 맞음. 코크스 강도는 코크스로 공정의 산출물 품질 지표라서 코크스로 쪽 데이터에서
#    나오고, 그 코크스가 실제로 부스러지는 현상은 그보다 한참 뒤 고로 안에서 일어남. 그래서
#    고로에서 통기성 악화 신호(송풍압 상승 등)가 보일 때 그 원인을 고로 데이터 안에서만 찾으면
#    안 되고, 며칠 전 그 코크스를 만든 코크스로 쪽 강도 데이터까지 거슬러 올라가 확인해야 함 -
#    "고로 데이터만 봐서는 안 풀리는 문제"라는 표현이 바로 이 상황을 가리킴


# =====================================================================
# 고로와 제선
# =====================================================================

# [개념] 고로 내부 흐름 - 향류 구조
# - 원료 장입(광석과 코크스를 층상으로 쌓아 아래 방향 흐름 형성) -> 예열(상승 가스 열로 미리
#   가열) -> 환원(일산화탄소가 산화철의 산소 제거) -> 용융(철과 슬래그가 녹아 하부로 이동) ->
#   출선(쇳물과 슬래그를 분리 배출)
# - 고로의 본질은 향류 구조: 원료는 아래로 내려가고 가스는 위로 올라가며 서로 반대 방향으로
#   만나 열과 반응 효율을 극대화 - 이 구조가 유지되려면 가스가 지나갈 통로가 끊기지 않아야 하므로
#   통기성 저하는 환원·용융 전체를 교란시킴

# [개념] 열풍로 - 열을 저장했다 내주는 설비
# - 축열(연료 가스로 벽돌 격자를 가열해 열을 저장) -> 송풍 전환(가열을 멈추고 찬 공기를 뜨거운
#   벽돌 사이로 통과) -> 열풍 공급(가열된 공기를 고로 송풍구로 공급, 다른 열풍로와 교대)

print("\n>>> 실제 고로 제선조업 시계열 데이터 (1분 간격 720행 = 12시간)")
ops = pd.read_csv("data/01-02_원료_전처리와_제선_제선조업.csv")
ops["timestamp"] = pd.to_datetime(ops["timestamp"])
print(ops.shape)
print(ops.describe().round(2))

plt.figure(figsize=(11, 4))
plt.plot(ops["timestamp"], ops["blast_flow_nm3min"], label="풍량(Nm3/min)")
plt.title("고로 송풍 풍량 추이 - 12시간")
plt.xlabel("시각")
plt.ylabel("풍량(Nm3/min)")
plt.xticks(rotation=30)
plt.tight_layout()
flow_path = os.path.join("data", "_tmp_steel_flow.png")
plt.savefig(flow_path)
plt.close()
print(os.path.exists(flow_path))  # True
os.remove(flow_path)

# [개념] 송풍 계통에서 나오는 값과 이상 시 해석
blow_signals = pd.DataFrame({
    "값": ["풍량", "송풍압", "노정압", "진동"],
    "구분": ["공정 상태", "공정 상태", "공정 상태", "설비 상태"],
    "이상 시 해석": ["감소 시 통기성 저하 의심", "상승 시 통기성 저하 의심",
                 "변동 폭 확대 시 가스가 고르게 못 올라가는 신호", "상승 시 회전부 열화 의심"],
})
print("\n>>> 송풍 계통 값과 이상 해석")
print(blow_signals.to_string(index=False))

print("\n>>> 다섯 센서 간 상관행렬")
corr = ops.drop(columns="timestamp").corr()
print(corr.round(2))

plt.figure(figsize=(7, 6))
sns.heatmap(corr, annot=True, fmt=".2f", cmap="coolwarm", center=0)
plt.title("고로 송풍 계통 상관관계")
plt.tight_layout()
heatmap_path = os.path.join("data", "_tmp_steel_heatmap.png")
plt.savefig(heatmap_path)
plt.close()
print(os.path.exists(heatmap_path))  # True
os.remove(heatmap_path)

# [퀴즈] 상관행렬에서 blast_flow_nm3min과 blast_pressure_kpa의 상관관계가 강할지 약할지,
# 그리고 두 값이 "같은 방향"으로 움직일지 먼저 예상해보기
print("\n>>> 정답 확인")
print(round(corr.loc["blast_flow_nm3min", "blast_pressure_kpa"], 3))
# 정답: -0.98, 매우 강한 음의 관계 - 직관과 반대 방향이라 눈여겨볼 결과
# (풍량이 늘수록 압력도 같이 오를 것 같지만, 실제로는 풍량이 늘어날 때 압력은 거의 정확히
#  반대로 움직임. 이런 -0.98처럼 1에 아주 가까운 상관계수는 대개 두 값이 우연히 함께 변한 게
#  아니라 같은 제어 로직에 함께 묶여 있다는 신호 - 예를 들어 목표 압력을 맞추도록 송풍량을
#  자동 조절하는 폐루프 제어가 걸려 있으면 풍량과 압력은 늘 서로를 밀어내는 짝으로 움직임.
#  중요한 건 부호(+/-)가 아니라 "두 값이 거의 완벽히 서로를 설명한다"는 사실 자체 - 평소엔
#  이렇게 강하게 얽혀 움직이던 두 값의 관계가 갑자기 느슨해지거나 부호가 뒤집힌다면, 그때가
#  바로 통기성 저하 같은 이상 신호로 의심해볼 지점)

print("\n>>> 노정압 박스플롯으로 변동성 확인")
q1 = ops["top_pressure_kpa"].quantile(0.25)
q3 = ops["top_pressure_kpa"].quantile(0.75)
iqr = q3 - q1
lower, upper = q1 - 1.5 * iqr, q3 + 1.5 * iqr
out_mask = (ops["top_pressure_kpa"] < lower) | (ops["top_pressure_kpa"] > upper)
print(round(iqr, 2), round(lower, 2), round(upper, 2), out_mask.sum())

sns.boxplot(y=ops["top_pressure_kpa"])
plt.title("노정압 분포")
plt.ylabel("노정압(kPa)")
plt.tight_layout()
box_path = os.path.join("data", "_tmp_steel_box.png")
plt.savefig(box_path)
plt.close()
print(os.path.exists(box_path))  # True
os.remove(box_path)

# [정리] 값 하나만 보고 판단하는 방식의 한계
# - 조업 조건에 따라 정상 범위 자체가 달라지므로, 이 12시간 동안의 IQR 기반 경계가 다른 날
#   그대로 적용되는 "절대 기준"은 아님 - 같은 설비의 평소 패턴과 비교하는 상대적 판단이 기본
# - 변한 값뿐 아니라 변하지 않은 값까지 함께 봐야 원인 후보가 좁혀짐 - 풍량·송풍압이 갈라지고
#   노정압 변동이 커지는데 송풍기 진동(blower_vib_mms)은 조용하다면, 송풍기 자체보다
#   고로 내부 통기성 저하 쪽을 먼저 의심하는 것이 합리적

# [개념] 쇳물은 어떻게 꺼내는가 - 출선 3단계
# - 출선구 천공(개공기로 막아 둔 출선구를 기계적으로 뚫음) -> 쇳물·슬래그 분리(대탕도 높이
#   차와 홈통 구조로 두 액상을 나눔) -> 출선구 폐쇄(머드건으로 내화 재료를 주입해 다시 봉쇄)
# - 고로 자체는 연속 공정이지만 출선만 놓고 보면 반복되는 사이클 - 고로 데이터에서 규칙적인
#   주기가 보이면 설비 이상이 아니라 출선 작업일 가능성부터 확인하는 것이 순서

# [정리] 상관관계보다 공정도가 먼저 - 이 단원 전체의 결론
# - 상관관계를 발견했을 때 "어느 쪽이 앞 공정인가"를 먼저 확인하는 습관이 통계 기법보다 우선
# - 새 현장에서는 데이터를 달라고 하기 전에 공정도부터 달라고 하기 - 어떤 컬럼이 어느 설비의
#   무엇이고, 이 구간에 조업이 있었는지, 라벨은 어느 시점 기준인지를 코드를 짜기 전에 먼저 확인
